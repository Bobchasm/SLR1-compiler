// 测试复杂的逻辑或表达式
int main() {
    int a = 1;
    int b = 0;
    int c = 2;
    
    // 这个应该可以工作
    if (a && b) {
        return 1;
    }
    
    // 这个可能有问题
    if (a && b || c > 0) {
        return 2;
    }
    
    return 0;
}