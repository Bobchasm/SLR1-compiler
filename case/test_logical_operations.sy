// 测试逻辑运算符
int main() {
    int a = 1;
    int b = 0;
    int c = 2;
    
    // 简单逻辑运算
    int result1 = a && b;  // 1 && 0 = 0
    int result2 = a || b;  // 1 || 0 = 1
    
    // 复杂逻辑表达式
    int result3 = (a && b) || (a + b >= 0);  // (1 && 0) || (1 + 0 >= 0) = 0 || 1 = 1
    int result4 = (a > 0 && b < 0) || (c != 0);  // (1 > 0 && 0 < 0) || (2 != 0) = (1 && 0) || 1 = 1
    
    // 更复杂的嵌套逻辑表达式
    if ((a > 0 && b >= 0) || (c <= 2 && a != b)) {
        result1 = 10;
    }
    
    // 混合算术和逻辑运算
    int complex_condition = (a + b > 0) && (c * 2 < 10) || (result1 == 0);
    
    // 使用逻辑表达式作为条件
    if ((a && b) || (c > 0)) {
        return 1;
    } else {
        return 0;
    }
}