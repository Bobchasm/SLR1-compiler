// 中端测试5: 全局变量
// 测试全局变量的声明和使用

int global_a = 100;
int global_b = 200;

int get_sum() {
    return global_a + global_b;
}

int main() {
    int local = get_sum();
    global_a = 50;
    
    if (local > 250) {
        return -1;
    }
    
    return 0;
}
