// 前端测试4: 一元运算符测试
/* 测试正号、负号、逻辑非 */

int main() {
    int a = 10;
    int b = -a;  // 负号
    int c = +a;  // 正号
    
    if (!0) {  // 逻辑非
        a = 1;
    }
    
    if (!a) {
        b = 2;
    }
    
    return 0;
}
