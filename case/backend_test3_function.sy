// 中端测试3: 函数调用
// 测试函数定义和参数传递

int add(int x, int y) {
    int result;
    result = x + y;
    return result;
}

int multiply(int a, int b) {
    return a * b;
}

int main() {
    int sum = add(10, 20);
    int product = multiply(5, 6);
    int total = sum + product;
    
    return total;
}
